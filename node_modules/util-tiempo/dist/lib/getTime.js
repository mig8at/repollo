"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const Validate_1 = __importDefault(require("../Validate"));
/**
 * Get the current time in the specified format.
 * @param options - An array of options or an object containing options.
 * @returns The current time as a string.
 */
const getTime = (...options) => {
    // Parse the options and validate them.
    const parseOptions = Validate_1.default.optionsTime(options);
    const timestamp = Validate_1.default.timestamp(parseOptions?.timestamp);
    const locale = Validate_1.default.locale(parseOptions?.local);
    const timezone = Validate_1.default.timezone(parseOptions?.timezone);
    const hour12 = Validate_1.default.hour12(parseOptions?.hour12);
    // Get the current time using the specified options.
    const currentTime = new Date(timestamp).toLocaleTimeString(locale, {
        timeZone: timezone,
        hour12,
    });
    return currentTime;
};
exports.default = getTime;
