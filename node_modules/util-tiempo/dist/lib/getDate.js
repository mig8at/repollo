"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const Validate_1 = __importDefault(require("../Validate"));
/**
 * Get the formatted date.
 * @param options - The options for formatting the date.
 * @returns The formatted date as a string.
 */
const getDate = (...options) => {
    // Parse the options and validate the date
    const parseOptions = Validate_1.default.optionsDate(options);
    const timestamp = Validate_1.default.timestamp(parseOptions?.timestamp);
    // Create a new Date object with the given timestamp
    const date = new Date(timestamp);
    // Get the formatted date string based on the locale and timezone
    const locale = Validate_1.default.locale(parseOptions?.local);
    const timezone = Validate_1.default.timezone(parseOptions?.timezone);
    const formattedDate = date.toLocaleDateString(locale, { timeZone: timezone });
    return formattedDate;
};
exports.default = getDate;
